'use strict';

function formatMailLike(username, ...otherParts) {
    if (otherParts.length === 0)
        return username;
    return `${username}@${otherParts.join('.')}`;
}

exports.formatMailLike = formatMailLike;
