'use strict';

var nameServiceAddress = require('./nameServiceAddress.js');

function isSameAddress(a, b) {
    if (nameServiceAddress.isNameServiceAddress(a) && nameServiceAddress.isNameServiceAddress(b))
        return nameServiceAddress.isSameNameServiceAddress(a, b);
    return false;
}

exports.isSameAddress = isSameAddress;
