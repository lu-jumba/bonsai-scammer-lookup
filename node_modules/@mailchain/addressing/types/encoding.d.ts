import { EncodingType } from '@mailchain/encoding';
import { ProtocolType } from './protocols';
/**
 * Convert address from Uint8Array, selects the relevant encoding method and encodes it as string.
 */
export declare function encodeAddressByProtocol(address: Uint8Array, protocol: ProtocolType): {
    encoded: string;
    encoding: EncodingType;
};
/**
 * Convert address from string, selects the relevant encoding method and decodes it as Uint8Array.
 */
export declare function decodeAddressByProtocol(address: string, protocol: ProtocolType): {
    decoded: Uint8Array;
    encoding: string;
};
/**
 * EncodingByProtocol returns the relevant encoding method the protocol commonly uses.
 */
export declare function encodingByProtocol(protocol: ProtocolType): EncodingType;
