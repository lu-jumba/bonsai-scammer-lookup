import { NameServiceAddress as MailchainAddress } from './nameServiceAddress';
import { ProtocolType } from './protocols';
/**
 * Helper method for creating of {@link MailchainAddress} for when the address is with specific protocol.
 * It provides the correct casing for the username.
 */
export declare function createWalletAddress(username: string, protocol: ProtocolType, mailchainAddressDomain: string): MailchainAddress;
