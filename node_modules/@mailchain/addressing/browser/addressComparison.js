import { isNameServiceAddress, isSameNameServiceAddress } from './nameServiceAddress.js';

function isSameAddress(a, b) {
    if (isNameServiceAddress(a) && isNameServiceAddress(b))
        return isSameNameServiceAddress(a, b);
    return false;
}

export { isSameAddress };
