import { PublicKey, Signer } from '@mailchain/crypto';
export type MailerProofParams = {
    authorContentSignature: Uint8Array;
    expires: Date;
    mailerMessagingKey: PublicKey;
};
export type MailerProof = {
    params: MailerProofParams;
    signature: Uint8Array;
    version: string;
};
export declare function createMailerProofSigningData(mailerProofParams: MailerProofParams, version: string): string;
export declare function signMailerProofParams(author: Signer, mailerProofParams: MailerProofParams, version: string): Promise<Uint8Array>;
export declare function createMailerProof(author: Signer, mailerProofParams: MailerProofParams, version: string): Promise<MailerProof>;
export declare function verifyMailerProof(author: PublicKey, mailerProof: MailerProof): Promise<boolean>;
