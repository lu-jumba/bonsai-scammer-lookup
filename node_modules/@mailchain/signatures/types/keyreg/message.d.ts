import { PublicKey } from '@mailchain/crypto';
import { ProofParams } from './params';
export declare function createProofMessage(params: ProofParams, address: Uint8Array, msgKey: PublicKey, nonce: number): string;
