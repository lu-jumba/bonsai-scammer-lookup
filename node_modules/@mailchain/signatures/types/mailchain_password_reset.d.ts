/// <reference types="node" />
import { PublicKey, Signer } from '@mailchain/crypto';
export declare const mailchainPasswordResetMessage: (username: Uint8Array, expires: Date) => Buffer;
/**
 * Signs a message using the Mailchain username, reset attestation message, and expiration with the identity private key.
 */
export declare function signMailchainPasswordReset(signer: Signer, username: Uint8Array, expires: Date): Promise<Uint8Array>;
/**
 * Verifies message linking a username, reset attestation message, and expiration with an identity key is valid.
 */
export declare function verifyMailchainPasswordReset(key: PublicKey, signature: Uint8Array, username: Uint8Array, expires: Date): Promise<boolean>;
