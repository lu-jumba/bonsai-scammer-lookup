function createMailboxAlias(address, params) {
    return {
        address,
        allowSending: params?.allowSending ?? true,
        allowReceiving: params?.allowReceiving ?? true,
    };
}

export { createMailboxAlias };
