import { AxiosInstance } from 'axios';
import { Payload } from '../../transport';
import { ReadonlyMailerPayload } from '../../transport/mailer/payload';
import { Configuration } from '../../configuration';
import { MailerAuthorVerifier } from './author';
export declare class MailerContentResolver {
    private readonly axiosInstance;
    private readonly sender;
    constructor(axiosInstance: AxiosInstance, sender: MailerAuthorVerifier);
    static create(configuration: Configuration, axiosInstance?: AxiosInstance): MailerContentResolver;
    get(payload: Payload): Promise<ReadonlyMailerPayload>;
}
