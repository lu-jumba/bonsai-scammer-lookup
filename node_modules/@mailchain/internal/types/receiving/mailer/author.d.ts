/// <reference types="node" />
import { Configuration } from '../../configuration';
import { MailSenderVerifier, Payload } from '../../transport';
export declare class MailerAuthorVerifier {
    private readonly mailSenderVerifier;
    constructor(mailSenderVerifier: MailSenderVerifier);
    static create(configuration: Configuration): MailerAuthorVerifier;
    /**
     * Checks if the author of the mailer is the same as the from address.
     * @param payload - The mailer payload
     * @returns
     */
    verifyAuthorOwnsFromAddress(payload: Payload, rfcMail: Buffer): Promise<boolean>;
}
