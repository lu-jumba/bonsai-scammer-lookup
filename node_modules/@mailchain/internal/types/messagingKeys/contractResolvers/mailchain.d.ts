import { ContractCall } from '@mailchain/api';
import { AxiosInstance } from 'axios';
import { Configuration } from '../../configuration';
import { MessagingKeyVerifier } from '../verify';
import { ContractCallLatestNonce, ContractCallMessagingKeyResolver, ContractCallResolveResult } from './resolver';
export declare class MailchainKeyRegContractCallResolver implements ContractCallMessagingKeyResolver, ContractCallLatestNonce {
    private readonly messagingKeyVerifier;
    private readonly rpcEndpoint;
    private readonly axiosInstance;
    constructor(messagingKeyVerifier: MessagingKeyVerifier, rpcEndpoint: string, axiosInstance: AxiosInstance);
    static create(configuration: Configuration, axiosInstance?: AxiosInstance): MailchainKeyRegContractCallResolver;
    resolve(contract: ContractCall): Promise<ContractCallResolveResult>;
    latestNonce(contract: ContractCall): Promise<number>;
    private callGetMessagingKeyContract;
}
