import { ResolvedAddress } from '../messagingKeys';
import { MailAddress, MailData } from '../transport';
export declare const createMimeMessage: (mailData: MailData, resolvedAddresses: Map<string, ResolvedAddress>) => Promise<{
    original: string;
    visibleRecipients: string;
    blindRecipients: {
        recipient: MailAddress;
        content: string;
    }[];
}>;
