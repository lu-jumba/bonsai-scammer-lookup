/// <reference types="node" />
import { ProtocolType } from '@mailchain/addressing';
import { PublicKey } from '@mailchain/crypto';
import { MailData } from '../transport';
export type ParseMimeTextResult = {
    mailData: MailData;
    addressIdentityKeys: Map<string, {
        identityKey: PublicKey;
        protocol: ProtocolType;
        network?: string;
    }>;
};
export declare function parseMimeText(content: Buffer): Promise<ParseMimeTextResult>;
