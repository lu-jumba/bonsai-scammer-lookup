import { NewUserMailbox } from './userProfile';
export declare function consolidateMailbox(mailbox: NewUserMailbox): NewUserMailbox;
export declare function consolidateMailboxLabel(mailbox: NewUserMailbox): NewUserMailbox;
/**
 * Consolidate the provided mailbox aliases with the following rules:
 * - There can be only single alias per `address`. If there are duplicates, only the first one in the entries is kept, the others are ignored.
 */
export declare function consolidateMailboxAliases(mailbox: NewUserMailbox): NewUserMailbox;
