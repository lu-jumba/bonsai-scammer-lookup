import { MailchainAddress } from '@mailchain/addressing';
import { Alias } from './types';
export declare function createMailboxAlias(address: MailchainAddress, params?: Partial<Omit<Alias, 'address'>>): Alias;
