import { PublicKey } from '@mailchain/crypto';
import { KeyRing } from '@mailchain/keyring';
import { MailData } from '../transport';
export type MessageIdCreator = (params: {
    mailData: MailData;
    type: 'sent';
} | {
    mailData: MailData;
    type: 'received';
    mailbox: PublicKey;
    owner: string;
}) => Promise<string>;
export declare function createMailchainMessageIdCreator(keyRing: KeyRing): MessageIdCreator;
