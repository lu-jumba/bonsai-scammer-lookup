import { PublicKey, RandomFunction } from '@mailchain/crypto';
import { protocol } from '../../protobuf/protocol/protocol';
export declare function createECDHKeyBundle(recipientMessagingKey: PublicKey, rand?: RandomFunction): Promise<{
    keyBundle: protocol.ECDHKeyBundle;
    secret: Uint8Array;
}>;
