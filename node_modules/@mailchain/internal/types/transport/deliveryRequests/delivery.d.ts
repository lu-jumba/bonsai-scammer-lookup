import { ExtendedPrivateKey, PublicKey, RandomFunction } from '@mailchain/crypto';
import { protocol } from '../../protobuf/protocol/protocol';
export declare function createDelivery(recipientMessagingKey: PublicKey, messageRootEncryptionKey: ExtendedPrivateKey, messageURI: string, rand?: RandomFunction): Promise<protocol.Delivery>;
