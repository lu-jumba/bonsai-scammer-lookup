import { ExtendedPrivateKey, PublicKey, RandomFunction } from '@mailchain/crypto';
import { protocol } from '../../protobuf/protocol/protocol';
/**
 *
 * @param recipientIdentityKey this is a Mailchain identity key.
 * @param messageKey root key used to encrypt message and payload
 */
export declare function createEnvelope(recipientMessagingKey: PublicKey, messageRootEncryptionKey: ExtendedPrivateKey, messageURI: string, rand?: RandomFunction): Promise<protocol.Envelope>;
