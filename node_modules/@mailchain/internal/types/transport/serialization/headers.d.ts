/// <reference types="node" />
import { PublicKey } from '@mailchain/crypto';
export interface SerializablePayloadHeaders {
    ToBuffer(): Buffer;
}
export declare function parseHeaderElements(input: string, requiredKeys: string[]): Map<string, string>;
export declare function parseSignatureHeader(input: string): Uint8Array;
export declare function createSignatureHeader(signature: Uint8Array, signer: PublicKey): string;
export declare function parseOriginHeader(input: string): PublicKey;
export declare function createOriginHeader(signer: PublicKey): string;
export declare function headersMapFromBuffers(buffer: Buffer, requiredHeaders: string[]): {
    headers: Map<string, string>;
    invalidHeaders: string[];
};
