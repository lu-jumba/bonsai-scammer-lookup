/// <reference types="node" />
import { EncryptedPayload } from './payload';
export declare enum MessageKind {
    UNSET = 0,
    HEADER = 1,
    CHUNK = 2,
    FINAL_CHUNK = 3
}
export declare function serializeMessage(encryptedMessage: Buffer, messageKind: MessageKind): Buffer;
export declare function serialize(input: EncryptedPayload): Buffer;
export declare function deserializeMessage(serializedMessage: Buffer): {
    kind: MessageKind;
    message: Buffer;
    remainingBuffer: Buffer;
};
export declare function deserialize(serializedData: Buffer): EncryptedPayload;
