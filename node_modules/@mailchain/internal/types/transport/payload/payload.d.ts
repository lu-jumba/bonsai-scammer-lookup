/// <reference types="node" />
import { ED25519ExtendedPrivateKey } from '@mailchain/crypto';
import { PayloadHeaders } from './headers';
/**
 * Payload.
 */
export interface Payload {
    /**
     * @see PayloadHeaders
     */
    Headers: PayloadHeaders;
    /**
     * Raw/data/object that are decrypted and parsed base on the the headers.
     */
    Content: Buffer;
}
export declare function serializeAndEncryptPayload(payload: Payload, payloadRootEncryptionKey: ED25519ExtendedPrivateKey): Promise<Buffer>;
