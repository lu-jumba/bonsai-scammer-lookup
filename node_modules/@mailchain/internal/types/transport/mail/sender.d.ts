import { PublicKey } from '@mailchain/crypto';
import { Configuration } from '../../configuration';
import { MessagingKeys } from '../../messagingKeys';
import { MailAddress } from './types';
export declare class MailSenderVerifier {
    private readonly messagingKeys;
    constructor(messagingKeys: MessagingKeys);
    static create(configuration: Configuration): MailSenderVerifier;
    /**
     *
     * @param fromAddress address that sent the mail. `From:` header in the mail.
     * @param senderMessagingKey public key of the sender.
     * @returns
     */
    verifySenderOwnsFromAddress(fromAddress: MailAddress, senderMessagingKey: PublicKey): Promise<boolean>;
}
