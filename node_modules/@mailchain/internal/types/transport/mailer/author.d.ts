import { Configuration } from '../../configuration';
import { MailSenderVerifier } from '../mail';
import { MailerContent } from './content';
export declare class MailerAuthorVerifier {
    private readonly sender;
    constructor(sender: MailSenderVerifier);
    static create(configuration: Configuration): MailerAuthorVerifier;
    verifyMailerAuthor(mailerContent: MailerContent): Promise<void>;
    private verifyAuthorWroteContent;
    private verifyAuthorOwnsFromAddress;
    private verifyMailerProof;
}
