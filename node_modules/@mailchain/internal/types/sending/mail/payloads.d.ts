import { SignerWithPublicKey } from '@mailchain/crypto';
import { MailDistribution, MailData, Payload } from '../../transport';
import { ResolvedAddress } from '../../messagingKeys';
export declare function createMailPayloads(senderMessagingKey: SignerWithPublicKey, resolvedAddresses: Map<string, ResolvedAddress>, mailData: MailData): Promise<{
    original: Payload;
    distributions: MailDistribution[];
}>;
export declare function createMailPayload(senderMessagingKey: SignerWithPublicKey, contentPayload: string): Promise<Payload>;
