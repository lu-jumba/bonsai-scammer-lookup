import { EncryptedContent, PlainContent, Decrypter } from '../cipher';
import { PrivateKey } from '../../private';
export declare class PrivateKeyDecrypter implements Decrypter {
    private _secretKey;
    private _keyId;
    constructor(privateKey: PrivateKey);
    static fromPrivateKey(key: PrivateKey): PrivateKeyDecrypter;
    decrypt(input: EncryptedContent): Promise<PlainContent>;
}
