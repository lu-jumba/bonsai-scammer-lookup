import { PublicKey } from '../../public';
import { EncryptedContent } from '../cipher';
export declare function serializePublicKeyEncryptedContent(sealedBox: EncryptedContent, pubKey: PublicKey): EncryptedContent;
export declare function serializePrivateKeyEncryptedContent(sealedBox: EncryptedContent, keyId: number): EncryptedContent;
export declare function deserializePublicKeyEncryptedContent(input: Uint8Array): {
    encryptedContent: EncryptedContent;
    pubKey: PublicKey;
};
export declare function deserializePrivateKeyEncryptedContent(input: Uint8Array): {
    encryptedContent: EncryptedContent;
    keyId: number;
};
