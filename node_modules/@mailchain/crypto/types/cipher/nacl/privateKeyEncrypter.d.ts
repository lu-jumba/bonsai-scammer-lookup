import { EncryptedContent, Encrypter } from '../cipher';
import { RandomFunction } from '../../rand';
import { PrivateKey } from '../../private';
export declare class PrivateKeyEncrypter implements Encrypter {
    private _rand;
    private _secretKey;
    private _keyId;
    constructor(privateKey: PrivateKey, rand?: RandomFunction);
    static fromPrivateKey(key: PrivateKey, rand?: RandomFunction): PrivateKeyEncrypter;
    encrypt(input: Uint8Array): Promise<EncryptedContent>;
}
