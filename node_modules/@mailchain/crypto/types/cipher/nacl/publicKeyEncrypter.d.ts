import { KeyExchange } from '../keyExchange';
import { EncryptedContent, Encrypter } from '../cipher';
import { RandomFunction } from '../../rand';
import { PublicKey } from '../../public';
export declare class PublicKeyEncrypter implements Encrypter {
    private _keyEx;
    private _pubKey;
    private _rand;
    constructor(keyEx: KeyExchange, pubKey: PublicKey, rand?: RandomFunction);
    static FromPublicKey(key: PublicKey): PublicKeyEncrypter;
    encrypt(input: Uint8Array): Promise<EncryptedContent>;
}
