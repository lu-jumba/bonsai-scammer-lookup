import { PublicKey } from '../public';
export declare const SECP256K1PublicKeyLength = 33;
export declare class SECP256K1PublicKey implements PublicKey {
    readonly bytes: Uint8Array;
    readonly curve: string;
    constructor(bytes: Uint8Array);
    /**
     * fromSignature will return a public key from the message and signature. The returned key
     * will validate the hash. Meaning message and signature pairs will return a public key.
     * Additionally checks should be performed to ensure the public key is as expected.
     * @param message
     * @param signature
     * @returns
     */
    static fromSignature(message: Uint8Array, signature: Uint8Array): Promise<SECP256K1PublicKey>;
    verify(message: Uint8Array, sig: Uint8Array): Promise<boolean>;
}
