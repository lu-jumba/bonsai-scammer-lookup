export interface PublicKey {
    readonly bytes: Uint8Array;
    verify: (message: Uint8Array, sig: Uint8Array) => Promise<boolean>;
    curve: string;
}
export declare function isPublicKeyEqual(a: PublicKey, b: PublicKey): boolean;
export declare function isPublicKey(x: any): x is PublicKey;
