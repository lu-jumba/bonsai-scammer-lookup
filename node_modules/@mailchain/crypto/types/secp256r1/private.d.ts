import { RandomFunction } from '../rand';
import { PrivateKey } from '../private';
import { SECP256R1PublicKey } from './public';
export declare const SECP256R1PrivateKeyLen = 32;
export declare class SECP256R1PrivateKey implements PrivateKey {
    readonly bytes: Uint8Array;
    private readonly rand;
    readonly publicKey: SECP256R1PublicKey;
    readonly curve: string;
    constructor(bytes: Uint8Array, rand?: RandomFunction);
    static generate(rand?: RandomFunction): SECP256R1PrivateKey;
    sign(message: Uint8Array): Promise<Uint8Array>;
}
