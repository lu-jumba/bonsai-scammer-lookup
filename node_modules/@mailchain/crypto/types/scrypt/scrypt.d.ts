export type ScryptParams = {
    N: number;
    r: number;
    p: number;
    keyLen: number;
};
export declare const defaultScryptParams: ScryptParams;
interface Result {
    params: ScryptParams;
    secret: Uint8Array;
    salt: Uint8Array;
}
export declare function deriveSecretFromScrypt(passphrase?: string, params?: ScryptParams, salt?: Uint8Array): Result;
export {};
