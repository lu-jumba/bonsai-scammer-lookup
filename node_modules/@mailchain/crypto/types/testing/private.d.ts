import { PublicKey } from '../public';
import { PrivateKey } from '../private';
export declare const PrivateKeyLen = 32;
export declare class UnknownPrivateKey implements PrivateKey {
    bytes: Uint8Array;
    publicKey: PublicKey;
    curve: string;
    constructor(bytes?: Uint8Array);
    sign(_message: Uint8Array): Promise<Uint8Array>;
}
