import { AxiosInstance } from 'axios';
import { SignerWithPublicKey } from '@mailchain/crypto';
export declare const getToken: (requestKey: SignerWithPublicKey, payload: TokenPayload, exp: number) => Promise<string>;
export declare const getAxiosWithSigner: (requestKey: SignerWithPublicKey) => AxiosInstance;
type TokenPayload = {
    /** The HTTP method */
    m: string;
    /** pathname */
    url: string;
    /** The length of the data payload of the request  */
    len: number;
    /** host */
    aud: string;
    /** query params */
    q?: string;
};
export declare function createTokenPayload(url: URL, method: string, data: any): TokenPayload;
export {};
