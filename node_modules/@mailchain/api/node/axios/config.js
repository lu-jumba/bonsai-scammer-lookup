'use strict';

var configuration = require('../api/configuration.js');

function createAxiosConfiguration(apiPath) {
    return new configuration.Configuration({
        basePath: apiPath,
    });
}

exports.createAxiosConfiguration = createAxiosConfiguration;
