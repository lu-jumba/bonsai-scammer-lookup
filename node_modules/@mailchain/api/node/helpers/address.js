'use strict';

var encoding = require('@mailchain/encoding');

const getAddressFromApiResponse = (address) => {
    return encoding.decode(address.encoding, address.value);
};

exports.getAddressFromApiResponse = getAddressFromApiResponse;
