import { Configuration } from '../api/configuration.js';

function createAxiosConfiguration(apiPath) {
    return new Configuration({
        basePath: apiPath,
    });
}

export { createAxiosConfiguration };
