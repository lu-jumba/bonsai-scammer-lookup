import { HEADER_LABELS } from './consts.js';

function createHeader(label, value, ...attrs) {
    return { label, value, attrs };
}
function createMessageIdHeader(label, ids, ...attrs) {
    return {
        label,
        value: {
            type: 'message-ids',
            ids,
        },
        attrs,
    };
}
function contentTypeBoundaryHeader(type, boundary) {
    return createHeader(HEADER_LABELS.ContentType, `multipart/${type}`, ['boundary', boundary]);
}

export { contentTypeBoundaryHeader, createHeader, createMessageIdHeader };
